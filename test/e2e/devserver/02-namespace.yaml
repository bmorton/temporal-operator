# Registers a namespace against the dev server via the polymorphic clusterRef.
apiVersion: temporal.bmor10.com/v1alpha1
kind: TemporalNamespace
metadata:
  name: orders
spec:
  clusterRef:
    name: dev
    kind: TemporalDevServer
  retentionPeriod: "24h"
  description: "Orders processing namespace"
