# Binaries
bin/
*.exe
*.test
*.out

# GoReleaser output (kept separate from the committed dist/ chart + manifests)
dist-goreleaser/

# Go
vendor/
cover.out
coverage.*

# Editors / OS
.idea/
.vscode/
*.swp
.DS_Store

# Local env / kube
kubeconfig
*.local.yaml

# Azure e2e harness (generated locally; never commit)
hack/.azure-e2e.env
hack/.entra-e2e.env

# Hugo docs site
docs/node_modules/
docs/public/
docs/resources/
.hugo_build.lock
docs/content/reference/.crd-reference-body.md
docs/content/examples/
.worktrees

# Generated WebAssembly resource-preview assets (rebuilt on every docs deploy)
docs/static/preview/temporal-operator-preview.wasm
docs/static/preview/wasm_exec.js
docs/static/preview/examples/
docs/data/preview.json
artifacts/

# kubebuilder's helm plugin resurrects this workflow; helmgen deletes it and
# it must never be committed. See issue #82.
.github/workflows/test-chart.yml

# Local `go build ./hack/helmgen/` drops this binary at the repo root.
/helmgen
