{{- if .Values.rbacHelpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
    labels:
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        app.kubernetes.io/name: temporal-operator
    name: temporal-operator-temporalcluster-admin-role
rules:
    - apiGroups:
        - temporal.bmor10.com
      resources:
        - temporalclusters
      verbs:
        - '*'
    - apiGroups:
        - temporal.bmor10.com
      resources:
        - temporalclusters/status
      verbs:
        - get
{{- end }}
