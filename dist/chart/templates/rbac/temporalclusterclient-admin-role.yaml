{{- if .Values.rbacHelpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
    labels:
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        app.kubernetes.io/name: temporal-operator
    name: temporal-operator-temporalclusterclient-admin-role
rules:
    - apiGroups:
        - temporal.bmor10.com
      resources:
        - temporalclusterclients
      verbs:
        - '*'
    - apiGroups:
        - temporal.bmor10.com
      resources:
        - temporalclusterclients/status
      verbs:
        - get
{{- end }}
