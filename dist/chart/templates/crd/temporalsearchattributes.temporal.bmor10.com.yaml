{{- if .Values.crd.enable }}
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
    annotations:
        controller-gen.kubebuilder.io/version: v0.19.0
    name: temporalsearchattributes.temporal.bmor10.com
spec:
    group: temporal.bmor10.com
    names:
        kind: TemporalSearchAttribute
        listKind: TemporalSearchAttributeList
        plural: temporalsearchattributes
        shortNames:
            - tsa
        singular: temporalsearchattribute
    scope: Namespaced
    versions:
        - additionalPrinterColumns:
            - jsonPath: .spec.clusterRef.name
              name: Cluster
              type: string
            - jsonPath: .spec.type
              name: Type
              type: string
            - jsonPath: .status.registered
              name: Registered
              type: boolean
            - jsonPath: .metadata.creationTimestamp
              name: Age
              type: date
          name: v1alpha1
          schema:
            openAPIV3Schema:
                description: TemporalSearchAttribute is the Schema for the temporalsearchattributes API.
                properties:
                    apiVersion:
                        description: |-
                            APIVersion defines the versioned schema of this representation of an object.
                            Servers should convert recognized schemas to the latest internal value, and
                            may reject unrecognized values.
                            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
                        type: string
                    kind:
                        description: |-
                            Kind is a string value representing the REST resource this object represents.
                            Servers may infer this from the endpoint the client submits requests to.
                            Cannot be updated.
                            In CamelCase.
                            More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
                        type: string
                    metadata:
                        type: object
                    spec:
                        description: spec defines the desired state of TemporalSearchAttribute
                        properties:
                            allowDeletion:
                                description: |-
                                    AllowDeletion permits the operator to remove the search attribute from the
                                    namespace when the CR is deleted.
                                type: boolean
                            clusterRef:
                                description: ClusterRef references the TemporalCluster or TemporalDevServer this search attribute belongs to.
                                properties:
                                    kind:
                                        default: TemporalCluster
                                        description: Kind selects the referenced object type.
                                        enum:
                                            - TemporalCluster
                                            - TemporalDevServer
                                        type: string
                                    name:
                                        description: Name is the name of the referenced object.
                                        type: string
                                required:
                                    - name
                                type: object
                            name:
                                description: Name is the search attribute name.
                                type: string
                            namespace:
                                description: Namespace is the Temporal namespace to register the attribute in.
                                type: string
                            type:
                                description: Type is the search attribute type. Immutable once created.
                                enum:
                                    - Keyword
                                    - Text
                                    - Int
                                    - Double
                                    - Bool
                                    - Datetime
                                    - KeywordList
                                type: string
                        required:
                            - clusterRef
                            - name
                            - namespace
                            - type
                        type: object
                    status:
                        description: status defines the observed state of TemporalSearchAttribute
                        properties:
                            conditions:
                                items:
                                    description: Condition contains details for one aspect of the current state of this API Resource.
                                    properties:
                                        lastTransitionTime:
                                            description: |-
                                                lastTransitionTime is the last time the condition transitioned from one status to another.
                                                This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                                            format: date-time
                                            type: string
                                        message:
                                            description: |-
                                                message is a human readable message indicating details about the transition.
                                                This may be an empty string.
                                            maxLength: 32768
                                            type: string
                                        observedGeneration:
                                            description: |-
                                                observedGeneration represents the .metadata.generation that the condition was set based upon.
                                                For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                                                with respect to the current state of the instance.
                                            format: int64
                                            minimum: 0
                                            type: integer
                                        reason:
                                            description: |-
                                                reason contains a programmatic identifier indicating the reason for the condition's last transition.
                                                Producers of specific condition types may define expected values and meanings for this field,
                                                and whether the values are considered a guaranteed API.
                                                The value should be a CamelCase string.
                                                This field may not be empty.
                                            maxLength: 1024
                                            minLength: 1
                                            pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                                            type: string
                                        status:
                                            description: status of the condition, one of True, False, Unknown.
                                            enum:
                                                - "True"
                                                - "False"
                                                - Unknown
                                            type: string
                                        type:
                                            description: type of condition in CamelCase or in foo.example.com/CamelCase.
                                            maxLength: 316
                                            pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                                            type: string
                                    required:
                                        - lastTransitionTime
                                        - message
                                        - reason
                                        - status
                                        - type
                                    type: object
                                type: array
                                x-kubernetes-list-map-keys:
                                    - type
                                x-kubernetes-list-type: map
                            observedGeneration:
                                format: int64
                                type: integer
                            registered:
                                description: Registered indicates whether the attribute has been registered with the cluster.
                                type: boolean
                            registeredAt:
                                description: RegisteredAt is when the attribute was registered.
                                format: date-time
                                type: string
                        type: object
                required:
                    - spec
                type: object
          served: true
          storage: true
          subresources:
            status: {}
{{- end }}
