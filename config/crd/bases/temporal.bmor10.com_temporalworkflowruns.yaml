---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: temporalworkflowruns.temporal.bmor10.com
spec:
  group: temporal.bmor10.com
  names:
    kind: TemporalWorkflowRun
    listKind: TemporalWorkflowRunList
    plural: temporalworkflowruns
    shortNames:
    - twr
    singular: temporalworkflowrun
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.clusterRef.name
      name: Cluster
      type: string
    - jsonPath: .spec.namespace
      name: Namespace
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TemporalWorkflowRun is the Schema for the temporalworkflowruns
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TemporalWorkflowRunSpec defines the desired state of a one-off
              workflow run.
            properties:
              cancellationPolicy:
                default: Abandon
                description: |-
                  CancellationPolicy controls what happens to a still-running workflow when
                  this CR is deleted.
                enum:
                - Abandon
                - Cancel
                - Terminate
                type: string
              clusterRef:
                description: |-
                  ClusterRef references the TemporalCluster or TemporalDevServer that runs
                  the workflow. Resolved in the same Kubernetes namespace as this CR.
                properties:
                  kind:
                    default: TemporalCluster
                    description: Kind selects the referenced object type.
                    enum:
                    - TemporalCluster
                    - TemporalDevServer
                    type: string
                  name:
                    description: Name is the name of the referenced object.
                    type: string
                required:
                - name
                type: object
              namespace:
                description: Namespace is the Temporal namespace to start the workflow
                  in.
                type: string
              ttlSecondsAfterFinished:
                description: |-
                  TTLSecondsAfterFinished, when set, deletes this CR that many seconds after
                  the workflow reaches a terminal state. Unset keeps the CR indefinitely.
                format: int32
                type: integer
              workflow:
                description: Workflow describes the one-off workflow to start. Immutable
                  after create.
                properties:
                  args:
                    description: Args are JSON-serializable workflow inputs (one json/plain
                      payload each).
                    items:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    type: array
                  memo:
                    additionalProperties:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    type: object
                  retryPolicy:
                    description: RetryPolicySpec is the retry policy for the started
                      workflow.
                    properties:
                      backoffCoefficient:
                        description: BackoffCoefficient is a decimal string (e.g.
                          "2.0") parsed to float64.
                        type: string
                      initialInterval:
                        type: string
                      maximumAttempts:
                        format: int32
                        type: integer
                      maximumInterval:
                        type: string
                      nonRetryableErrorTypes:
                        items:
                          type: string
                        type: array
                    type: object
                  searchAttributes:
                    additionalProperties:
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    type: object
                  taskQueue:
                    type: string
                  workflowExecutionTimeout:
                    type: string
                  workflowID:
                    type: string
                  workflowIDReusePolicy:
                    enum:
                    - AllowDuplicate
                    - AllowDuplicateFailedOnly
                    - RejectDuplicate
                    - TerminateIfRunning
                    type: string
                  workflowRunTimeout:
                    type: string
                  workflowTaskTimeout:
                    type: string
                  workflowType:
                    type: string
                required:
                - taskQueue
                - workflowType
                type: object
            required:
            - clusterRef
            - namespace
            - workflow
            type: object
          status:
            description: TemporalWorkflowRunStatus is the observed state of a workflow
              run.
            properties:
              closeTime:
                format: date-time
                type: string
              completionTime:
                description: |-
                  CompletionTime is when the operator first observed a terminal state; it
                  drives the TTL countdown.
                format: date-time
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              failure:
                description: Failure carries the failure message/type for non-success
                  terminal states.
                properties:
                  message:
                    type: string
                  type:
                    type: string
                type: object
              historyLength:
                format: int64
                type: integer
              observedGeneration:
                format: int64
                type: integer
              phase:
                description: |-
                  Phase is a friendly lifecycle summary: Pending, Running, Completed,
                  Failed, Terminated, Canceled, TimedOut, or ContinuedAsNew.
                type: string
              runID:
                type: string
              startTime:
                format: date-time
                type: string
              taskQueue:
                type: string
              workflowID:
                type: string
              workflowType:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
